[package]
name = "rge1"
version = "0.3.0"
edition = "2024"

[dependencies]
argh = { version = "0.1.19", default-features = false, features = ["help"] }
fastrand = "2.4.1"
rayon = "1.12.0"

[profile.dev]
debug = 0

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
